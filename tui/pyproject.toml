[project]
name = "limedb-tui"
version = "0.1.0"
description = "TUI for LimeDB Cluster"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.40.0",
    "aiohttp>=3.9.0",
]

